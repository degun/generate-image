[tool.poetry]
name = "img-api"
version = "0.1.0"
description = ""
authors = ["degun <edlir.velaj@outlook.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
flask = "^3.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
